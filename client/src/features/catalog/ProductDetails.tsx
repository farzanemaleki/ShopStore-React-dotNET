import { Typography } from "@mui/material";

export default function ProductsDetails () {
    return(
        <>
        <Typography variant="h2">
            Product Details
        </Typography>
        </>
    );
}